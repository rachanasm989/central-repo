#include <stdio.h>
    
    void biggest2()
    {
        int num1, num2;
    
        printf("\nEnter two integers :");
        scanf("%d%d", &num1, &num2);
    
        if (num1 > num2)
        {
	        printf("\nLargest number is %d.", num1);
	    }
        else
        {
	        printf("\Largest number is %d.\n", num2);
	    }
    
     //   return 0;
    }
